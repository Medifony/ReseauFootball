{% extends 'base.html.twig' %}

{% block title %}ReseauFootball - Accueil{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-6">
                <h1 style="text-align:center; margin-top: 100px;">Bienvenue !</h1>
                <div style="text-align:center; margin-top: 50px;">{% if liga %}{% if liga.payss %}<img src="https://www.countryflags.io/{{liga.payss.alpha2}}/flat/48.png">{% endif %}{% endif %}
                    {% if premL %}{% if premL.payss %}<img src="https://www.countryflags.io/{{premL.payss.alpha2}}/flat/48.png">{% endif %}{% endif %}
                    {% if bundesliga %}{% if bundesliga.payss %}<img src="https://www.countryflags.io/{{bundesliga.payss.alpha2}}/flat/48.png">{% endif %}{% endif %}
                    {% if serieA %}{% if serieA.payss %}<img src="https://www.countryflags.io/{{serieA.payss.alpha2}}/flat/48.png">{% endif %}{% endif %}
                    {% if ligueUn %}{% if ligueUn.payss %}<img src="https://www.countryflags.io/{{ligueUn.payss.alpha2}}/flat/48.png">{% endif %}{% endif %}
                </div>
                <h1 style="margin-top: 50px; text-align:center;">Consultez les derniers topics postés sur notre site.</h1>
            </div>
            {% if liga %}
            <div class="col-6">
                <table class="table" border=1>
                    <thead>
                        <tr>
                            <th colspan=3 class="text-center"> <img src="{{liga.logo}}" class="logoNav"> <strong>{{ liga.nom }}</strong> <img src="{{liga.logo}}" class="logoNav"></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% if sujetsLiga %}
                        {% set col = 0 %}
                        {% for sujet in sujetsLiga %}
                        {% if sujet.championnats %}
                            <tr {% if  col  is divisible by(2) %}style="background-color: azure"{% endif %}>
                                <td width=5% class="text-center">
                                    {% if sujet.users %}
                                        {% if sujet.users.filename %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="{{ vich_uploader_asset(sujet.users, 'imageFile') }}" class="avatarHome"></a>
                                        {% else %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="/images/avatarVide.jpg" class="avatarHome"></a>
                                        {% endif %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><strong>{{sujet.users.pseudo}}</strong></a>
                                    {% else %}
                                        <img src="/images/avatarVide.jpg" class="avatarHome"> <strong><i>Supprimé</i></strong>
                                    {% endif %}
                                </td>
                                <td width=50% style="vertical-align : middle;text-align:center;">
                                    {% if sujet.clubs %}
                                        <a href="{{ path ('sujet_club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                    {% else %}
                                        <a href="{{ path ('sujet_champ_page', {'slugCh' : sujet.championnats.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                    {% endif %}
                                </td>
                                <td width=20% class="text-center">
                                    {% if sujet.clubs %}
                                        <a class="nav-link" href="{{ path ('club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug}) }}"><img src="{{sujet.clubs.logo}}" class="logoNav"></br>{{sujet.clubs.navbar}}</a>
                                    {% else %}
                                        <a href="{{ path ('champ_page', {'slug' : sujet.championnats.slug}) }}"><img src="{{sujet.championnats.logo}}" class="logoNav"></br>{{sujet.championnats.nom}}</a>
                                    {% endif %}
                                </td>
                            </tr>
                            {% set col = col + 1 %}
                            {% endif %}
                        {% endfor %}
                        {% else %}
                            <tr>
                                <td><h3 style="color:red">Aucun sujet posté pour le moment.</h3></td>
                            </tr>
                        {% endif %}
                    </tbody>
                </table>
            </div>
            {% endif %}
        </div>
        <div class="row">
            {% if premL %}
            <div class="col-6">
                <table class="table" border=1>
                    <thead>
                        <tr>
                            <th colspan=3 class="text-center"> <img src="{{premL.logo}}" class="logoNav"> <strong>{{ premL.nom }}</strong> <img src="{{premL.logo}}" class="logoNav"></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% if sujetsPl %}
                        {% set col = 0 %}
                        {% for sujet in sujetsPl %}
                            {% if sujet.championnats %}
                            <tr {% if  col  is divisible by(2) %}style="background-color: azure"{% endif %}>
                                <td width=5% class="text-center">
                                    {% if sujet.users %}
                                        {% if sujet.users.filename %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="{{ vich_uploader_asset(sujet.users, 'imageFile') }}" class="avatarHome"></a>
                                        {% else %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="/images/avatarVide.jpg" class="avatarHome"></a>
                                        {% endif %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><strong>{{sujet.users.pseudo}}</strong></a>
                                    {% else %}
                                        <img src="/images/avatarVide.jpg" class="avatarHome"> <strong><i>Supprimé</i></strong>
                                    {% endif %}
                                </td>
                                <td width=50% style="vertical-align : middle;text-align:center;">
                                    {% if sujet.clubs %}
                                        <a href="{{ path ('sujet_club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                    {% else %}
                                        <a href="{{ path ('sujet_champ_page', {'slugCh' : sujet.championnats.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                    {% endif %}
                                </td>
                                <td width=20% class="text-center">
                                    {% if sujet.clubs %}
                                        <a class="nav-link" href="{{ path ('club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug}) }}"><img src="{{sujet.clubs.logo}}" class="logoNav"></br>{{sujet.clubs.navbar}}</a>
                                    {% else %}
                                        <a href="{{ path ('champ_page', {'slug' : sujet.championnats.slug}) }}"><img src="{{sujet.championnats.logo}}" class="logoNav"></br>{{sujet.championnats.nom}}</a>
                                    {% endif %}
                                </td>
                            </tr>
                            {% set col = col + 1 %}
                            {% endif %}
                        {% endfor %}
                        {% else %}
                            <tr>
                                <td><h3 style="color:red">Aucun sujet posté pour le moment.</h3></td>
                            </tr>
                        {% endif %}
                    </tbody>
                </table>
            </div>
            {% endif %}
            {% if bundesliga %}
            <div class="col-6">
                <table class="table" border=1>
                        <thead>
                            <tr>
                                <th colspan=3 class="text-center"> <img src="{{bundesliga.logo}}" class="logoNav"> <strong>{{ bundesliga.nom }}</strong> <img src="{{bundesliga.logo}}" class="logoNav"></th>
                            </tr>
                        </thead>
                        <tbody>
                            {% if sujetsBundes %}
                            {% set col = 0 %}
                            {% for sujet in sujetsBundes %}
                            {% if sujet.championnats %}
                                <tr {% if  col  is divisible by(2) %}style="background-color: azure"{% endif %}>
                                    <td width=5% class="text-center">
                                    {% if sujet.users %}
                                        {% if sujet.users.filename %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="{{ vich_uploader_asset(sujet.users, 'imageFile') }}" class="avatarHome"></a>
                                        {% else %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="/images/avatarVide.jpg" class="avatarHome"></a>
                                        {% endif %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><strong>{{sujet.users.pseudo}}</strong></a>
                                    {% else %}
                                        <img src="/images/avatarVide.jpg" class="avatarHome"> <strong><i>Supprimé</i></strong>
                                    {% endif %}
                                    </td>
                                    <td width=50% style="vertical-align : middle;text-align:center;">
                                        {% if sujet.clubs %}
                                            <a href="{{ path ('sujet_club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                        {% else %}
                                            <a href="{{ path ('sujet_champ_page', {'slugCh' : sujet.championnats.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                        {% endif %}
                                    </td>
                                    <td width=20% class="text-center">
                                        {% if sujet.clubs %}
                                            <a class="nav-link" href="{{ path ('club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug}) }}"><img src="{{sujet.clubs.logo}}" class="logoNav"></br>{{sujet.clubs.navbar}}</a>
                                        {% else %}
                                            <a href="{{ path ('champ_page', {'slug' : sujet.championnats.slug}) }}"><img src="{{sujet.championnats.logo}}" class="logoNav"></br>{{sujet.championnats.nom}}</a>
                                        {% endif %}
                                    </td>
                                </tr>
                                {% set col = col + 1 %}
                                {% endif %}
                            {% endfor %}
                            {% else %}
                            <tr>
                                <td><h3 style="color:red">Aucun sujet posté pour le moment.</h3></td>
                            </tr>
                        {% endif %}
                        </tbody>
                    </table>
            </div>
            {% endif %}
        </div>
        <div class="row">
            {% if serieA %}
            <div class="col-6">
                <table class="table" border=1>
                    <thead>
                        <tr>
                            <th colspan=3 class="text-center"> <img src="{{serieA.logo}}" class="logoNav"> <strong>{{ serieA.nom }}</strong> <img src="{{serieA.logo}}" class="logoNav"></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% if sujetsSerieA %}
                        {% set col = 0 %}
                        {% for sujet in sujetsSerieA %}
                        {% if sujet.championnats %}
                            <tr {% if  col  is divisible by(2) %}style="background-color: azure"{% endif %}>
                                <td width=5% class="text-center">
                                    {% if sujet.users %}
                                        {% if sujet.users.filename %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="{{ vich_uploader_asset(sujet.users, 'imageFile') }}" class="avatarHome"></a>
                                        {% else %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="/images/avatarVide.jpg" class="avatarHome"></a>
                                        {% endif %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><strong>{{sujet.users.pseudo}}</strong></a>
                                    {% else %}
                                        <img src="/images/avatarVide.jpg" class="avatarHome"> <strong><i>Supprimé</i></strong>
                                    {% endif %}
                                </td>
                                <td width=50% style="vertical-align : middle;text-align:center;">
                                    {% if sujet.clubs %}
                                        <a href="{{ path ('sujet_club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                    {% else %}
                                        <a href="{{ path ('sujet_champ_page', {'slugCh' : sujet.championnats.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                    {% endif %}
                                </td>
                                <td width=20% class="text-center">
                                    {% if sujet.clubs %}
                                        <a class="nav-link" href="{{ path ('club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug}) }}"><img src="{{sujet.clubs.logo}}" class="logoNav"></br>{{sujet.clubs.navbar}}</a>
                                    {% else %}
                                        <a href="{{ path ('champ_page', {'slug' : sujet.championnats.slug}) }}"><img src="{{sujet.championnats.logo}}" class="logoNav"></br>{{sujet.championnats.nom}}</a>
                                    {% endif %}
                                </td>
                            </tr>
                            {% set col = col + 1 %}
                            {% endif %}
                        {% endfor %}
                        {% else %}
                            <tr>
                                <td><h3 style="color:red">Aucun sujet posté pour le moment.</h3></td>
                            </tr>
                        {% endif %}
                    </tbody>
                </table>
            </div>
            {% endif %}
            {% if ligueUn %}
            <div class="col-6">
                <table class="table" border=1>
                    <thead>
                        <tr>
                            <th colspan=3 class="text-center"> <img src="{{ligueUn.logo}}" class="logoNav"> <strong>{{ ligueUn.nom }}</strong> <img src="{{ligueUn.logo}}" class="logoNav"></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% if sujetsLigueUn %}
                        {% set col = 0 %}
                        {% for sujet in sujetsLigueUn %}
                        {% if sujet.championnats %}
                            <tr {% if  col  is divisible by(2) %}style="background-color: azure"{% endif %}>
                                <td width=5% class="text-center">
                                    {% if sujet.users %}
                                        {% if sujet.users.filename %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="{{ vich_uploader_asset(sujet.users, 'imageFile') }}" class="avatarHome"></a>
                                        {% else %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><img src="/images/avatarVide.jpg" class="avatarHome"></a>
                                        {% endif %}
                                            <a href="{{ path ('user_show', {'slug' : sujet.users.slug}) }}"><strong>{{sujet.users.pseudo}}</strong></a>
                                    {% else %}
                                        <img src="/images/avatarVide.jpg" class="avatarHome"> <strong><i>Supprimé</i></strong>
                                    {% endif %}
                                </td>
                                <td width=50% style="vertical-align : middle;text-align:center;">
                                    {% if sujet.clubs %}
                                        <a href="{{ path ('sujet_club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                    {% else %}
                                        <a href="{{ path ('sujet_champ_page', {'slugCh' : sujet.championnats.slug, 'slugSuj' : sujet.slug}) }}"><i>{{sujet.titre}}</i></a>
                                    {% endif %}
                                </td>
                                <td width=20% class="text-center">
                                    {% if sujet.clubs %}
                                        <a class="nav-link" href="{{ path ('club_page', {'slugCh' : sujet.championnats.slug, 'slugCl' : sujet.clubs.slug}) }}"><img src="{{sujet.clubs.logo}}" class="logoNav"></br>{{sujet.clubs.navbar}}</a>
                                    {% else %}
                                        <a href="{{ path ('champ_page', {'slug' : sujet.championnats.slug}) }}"><img src="{{sujet.championnats.logo}}" class="logoNav"></br>{{sujet.championnats.nom}}</a>
                                    {% endif %}
                                </td>
                            </tr>
                            {% set col = col + 1 %}
                            {% endif %}
                        {% endfor %}
                        {% else %}
                            <tr>
                                <td><h3 style="color:red">Aucun sujet posté pour le moment.</h3></td>
                            </tr>
                        {% endif %}
                    </tbody>
                </table>
            </div>
            {% endif %}
    </div>

{% endblock %}
