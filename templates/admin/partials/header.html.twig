<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="{{path('admin_api_index')}}">Api</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_championnats_index')}}"><i class="fas fa-futbol"></i> <strong>Championnats</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_sujets_index')}}"><i class="fas fa-bookmark"></i> <strong>Sujets</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_reponses_index')}}"><i class="far fa-bookmark"></i> <strong>Reponses</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_clubs_index')}}"><i class="far fa-futbol"></i> <strong>Clubs</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_users_index')}}"><i class="fas fa-users"></i> <strong>Users</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_messages_index')}}"><i class="fas fa-envelope"></i> <strong>Messages</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_conversations_index')}}"><i class="fas fa-comments"></i> <strong>Conversations</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_pays_index')}}"><i class="fas fa-flag"></i> <strong>Pays</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_amis_index')}}"><i class="fas fa-user-friends"></i> <strong>Amis</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_classements_index')}}"><i class="fas fa-trophy"></i> <strong>Classements</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_rencontres_index')}}"><i class="fas fa-futbol"></i> <strong>Rencontres</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_signalements_index')}}"><i class="fas fa-exclamation-triangle"></i> <strong>Signalements</strong></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_roles_index')}}"><i class="fas fa-user-friends"></i> <strong>Roles</strong></a>
                </li>
            <li class="nav-item">
                    <a class="nav-link"></a>
                </li>
            </ul>

            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('homepage') }}" target="_blank" class="nav-link">
                        <i class="fas fa-eye"></i>
                        Voir le site
                    </a>
                </li>
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                            {% if app.user.filename %}
                                <img src="{{ vich_uploader_asset(app.user, 'imageFile') }}" class="avatarDrop">
                            {% else %}
                                <img src="/images/avatarVide.jpg" class="avatarDrop">
                            {% endif %} <strong>{{app.user.Pseudo}}</strong>
                        </a>

                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                            <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
{% for label, messages in app.flashes %}
    <div class="container">
        <div class="row">
        <div class="col-offset-2 col-8">
        <div class="alert alert-{{label}}">
            {% for message in messages %}
                <p>{{message | raw}}</p>
            {% endfor %}
        </div>
        </div>
        </div>
    </div>
{% endfor %}